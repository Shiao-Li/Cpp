//ahora se imprimira una linea de texto con varias instrucciones
#include<iostream> //permite la impresion de datos en el programa (io=input/output de datos)

// es la fucion principal que para la ejecucion del programa 
int main()
{
    std::cout <<"bienvenido "; //muestra el mensaje requerido
    std::cout <<"a c++ :D\n";
    return 0; //indica que el programa ya se finalizo 

} // finaliza la funcion main